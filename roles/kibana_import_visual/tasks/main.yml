---
  - name: Import my_dashboard to kibana
    uri:
      url: http://{{groups['visualserver'][0]}}:5601/api/saved_objects/_bulk_create
      method: POST
      body: "{{ lookup('file','m.json') }}"
      headers:
        'kbn-xsrf': 'true'
      force_basic_auth: yes
      status_code: 200
      body_format: json